local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera

local TargetPlayer = nil
local AimbotEnabled = false
local TeleportEnabled = false
local HitboxEnabled = false
local WasHovering = false
local OriginalHeadSize = nil

-- ==========================================
-- 1. CREACIÓN DEL MENÚ (UI)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PlayerSelectorGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = PlayerGui

local Frame = Instance.new("ScrollingFrame")
Frame.Size = UDim2.new(0, 200, 0, 400) -- Más largo para acomodar el nuevo botón
Frame.Position = UDim2.new(0, 50, 0, 100)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BorderSizePixel = 0
Frame.ScrollBarImageColor3 = Color3.fromRGB(150, 150, 150)
Frame.AutomaticCanvasSize = Enum.AutomaticSize.Y 
Frame.CanvasSize = UDim2.new(0, 0, 0, 0)
Frame.Parent = ScreenGui
Frame.Active = true
Frame.Draggable = true 

local UICornerFrame = Instance.new("UICorner")
UICornerFrame.CornerRadius = UDim.new(0, 8)
UICornerFrame.Parent = Frame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder 
UIListLayout.Parent = Frame

local UIPadding = Instance.new("UIPadding")
UIPadding.PaddingTop = UDim.new(0, 5)
UIPadding.PaddingBottom = UDim.new(0, 5)
UIPadding.Parent = Frame

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.RightControl then
        Frame.Visible = not Frame.Visible
    end
end)

-- ==========================================
-- 2. BOTONES MAESTROS (TOGGLES)
-- ==========================================
-- Botón AIMBOT
local btnAimbot = Instance.new("TextButton")
btnAimbot.Size = UDim2.new(1, -10, 0, 35)
btnAimbot.Text = "AIMBOT: OFF"
btnAimbot.Font = Enum.Font.GothamBlack
btnAimbot.TextColor3 = Color3.fromRGB(255, 255, 255)
btnAimbot.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
btnAimbot.LayoutOrder = 1
local corner1 = Instance.new("UICorner") corner1.CornerRadius = UDim.new(0, 6) corner1.Parent = btnAimbot
btnAimbot.Parent = Frame

btnAimbot.MouseButton1Click:Connect(function()
    AimbotEnabled = not AimbotEnabled
    btnAimbot.Text = AimbotEnabled and "AIMBOT: ON" or "AIMBOT: OFF"
    btnAimbot.BackgroundColor3 = AimbotEnabled and Color3.fromRGB(180, 20, 20) or Color3.fromRGB(45, 45, 45)
end)

-- Botón TELEPORT
local btnTeleport = Instance.new("TextButton")
btnTeleport.Size = UDim2.new(1, -10, 0, 35)
btnTeleport.Text = "TELEPORT: OFF"
btnTeleport.Font = Enum.Font.GothamBlack
btnTeleport.TextColor3 = Color3.fromRGB(255, 255, 255)
btnTeleport.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
btnTeleport.LayoutOrder = 2
local corner2 = Instance.new("UICorner") corner2.CornerRadius = UDim.new(0, 6) corner2.Parent = btnTeleport
btnTeleport.Parent = Frame

btnTeleport.MouseButton1Click:Connect(function()
    TeleportEnabled = not TeleportEnabled
    btnTeleport.Text = TeleportEnabled and "TELEPORT: ON" or "TELEPORT: OFF"
    btnTeleport.BackgroundColor3 = TeleportEnabled and Color3.fromRGB(40, 150, 200) or Color3.fromRGB(45, 45, 45)
end)

-- Botón HITBOX EXPANDER
local btnHitbox = Instance.new("TextButton")
btnHitbox.Size = UDim2.new(1, -10, 0, 35)
btnHitbox.Text = "BIG HITBOX: OFF"
btnHitbox.Font = Enum.Font.GothamBlack
btnHitbox.TextColor3 = Color3.fromRGB(255, 255, 255)
btnHitbox.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
btnHitbox.LayoutOrder = 3
local corner3 = Instance.new("UICorner") corner3.CornerRadius = UDim.new(0, 6) corner3.Parent = btnHitbox
btnHitbox.Parent = Frame

btnHitbox.MouseButton1Click:Connect(function()
    HitboxEnabled = not HitboxEnabled
    btnHitbox.Text = HitboxEnabled and "BIG HITBOX: ON" or "BIG HITBOX: OFF"
    btnHitbox.BackgroundColor3 = HitboxEnabled and Color3.fromRGB(200, 150, 40) or Color3.fromRGB(45, 45, 45)
    
    -- Si lo apagamos, tratamos de devolver la cabeza a la normalidad al instante
    if not HitboxEnabled and TargetPlayer and TargetPlayer.Character then
        local head = TargetPlayer.Character:FindFirstChild("Head")
        if head then
            head.Size = OriginalHeadSize or Vector3.new(1.2, 1.2, 1.2)
            head.Transparency = 0 -- Lo vuelve visible normal
        end
    end
end)

local Separator = Instance.new("Frame")
Separator.Size = UDim2.new(1, -20, 0, 2)
Separator.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
Separator.LayoutOrder = 4
Separator.Parent = Frame

-- ==========================================
-- 3. LISTA DE JUGADORES
-- ==========================================
local function createPlayerButton(player)
    if Frame:FindFirstChild(player.Name) then return end 

    local btn = Instance.new("TextButton")
    btn.Name = player.Name
    btn.Size = UDim2.new(1, -10, 0, 30)
    btn.Text = player.Name
    btn.Font = Enum.Font.GothamSemibold
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    btn.LayoutOrder = 5 
    local cornerBtn = Instance.new("UICorner") cornerBtn.CornerRadius = UDim.new(0, 6) cornerBtn.Parent = btn
    btn.Parent = Frame

    btn.MouseButton1Click:Connect(function()
        -- Restaurar cabeza anterior si cambiamos de objetivo
        if TargetPlayer and TargetPlayer.Character then
            if TargetPlayer.Character:FindFirstChild("KillHighlight") then
                TargetPlayer.Character.KillHighlight:Destroy()
            end
            local oldHead = TargetPlayer.Character:FindFirstChild("Head")
            if oldHead then
                oldHead.Size = OriginalHeadSize or Vector3.new(1.2, 1.2, 1.2)
                oldHead.Transparency = 0
            end
        end

        if TargetPlayer == player then
            TargetPlayer = nil
            btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        else
            for _, child in ipairs(Frame:GetChildren()) do
                if child:IsA("TextButton") and child.LayoutOrder == 5 then
                    child.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
                end
            end
            
            TargetPlayer = player
            btn.BackgroundColor3 = Color3.fromRGB(40, 200, 70) 
            
            -- Guardar tamaño original de la cabeza del nuevo objetivo
            if TargetPlayer.Character and TargetPlayer.Character:FindFirstChild("Head") then
                OriginalHeadSize = TargetPlayer.Character.Head.Size
            end
        end
    end)
end

for _, plr in pairs(Players:GetPlayers()) do
    if plr ~= LocalPlayer then createPlayerButton(plr) end
end
Players.PlayerAdded:Connect(createPlayerButton)
Players.PlayerRemoving:Connect(function(plr)
    if TargetPlayer == plr then TargetPlayer = nil end
    local button = Frame:FindFirstChild(plr.Name)
    if button then button:Destroy() end
end)

-- ==========================================
-- 4. RESTAURAR PERSONAJE PROPIO
-- ==========================================
local function restoreCharacter()
    local char = LocalPlayer.Character
    if char then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                if part.Name ~= "HumanoidRootPart" then part.Transparency = 0 end
                part.CanCollide = true
                part.Massless = false
            elseif part:IsA("Decal") and part.Name == "face" then
                part.Transparency = 0
            end
        end
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then root.Anchored = false end
    end
end

-- ==========================================
-- 5. AIMBOT PURO (CÁMARA)
-- ==========================================
RunService:BindToRenderStep("LethalAimbot", 201, function()
    if AimbotEnabled and TargetPlayer and TargetPlayer.Character then
        local targetHead = TargetPlayer.Character:FindFirstChild("Head")
        if targetHead then
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetHead.Position)
        end
    end
end)

-- ==========================================
-- 6. MOTOR DE COMBATE COMPLETO (Teleport + Hitbox)
-- ==========================================
RunService.Heartbeat:Connect(function()
    local localCharacter = LocalPlayer.Character
    local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
    
    local targetCharacter = TargetPlayer and TargetPlayer.Character
    local targetRoot = targetCharacter and targetCharacter:FindFirstChild("HumanoidRootPart")
    local targetHead = targetCharacter and targetCharacter:FindFirstChild("Head")

    -- 6.1 LÓGICA DE HITBOX EXPANDER (CABEZÓN)
    if targetHead then
        if HitboxEnabled then
            -- Forzamos la cabeza a ser un bloque gigante rojo y semi-transparente
            targetHead.Size = Vector3.new(12, 12, 12)
            targetHead.Transparency = 0.7
            targetHead.Color = Color3.fromRGB(255, 0, 0)
            targetHead.CanCollide = false
            targetHead.Massless = true
        else
            -- Para evitar que se quede cabezón si se apaga de golpe
            targetHead.Size = OriginalHeadSize or Vector3.new(1.2, 1.2, 1.2)
        end
    end

    -- 6.2 LÓGICA DE TELEPORT Y ANTI-COLISIÓN
    if localRoot then
        if TeleportEnabled then
            WasHovering = true
            localRoot.Anchored = true
            
            for _, part in ipairs(localCharacter:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    part.Transparency = 1
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                        part.Massless = true
                    end
                end
            end
            
            if targetRoot then
                if not targetCharacter:FindFirstChild("KillHighlight") then
                    local highlight = Instance.new("Highlight")
                    highlight.Name = "KillHighlight"
                    highlight.FillColor = Color3.fromRGB(255, 0, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 0.1
                    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    highlight.Parent = targetCharacter
                end
                
                local targetPos = targetRoot.Position
                local intendedPos = (targetRoot.CFrame * CFrame.new(0, 8, 3)).Position
                
                local rayDirection = intendedPos - targetPos
                local raycastParams = RaycastParams.new()
                raycastParams.FilterDescendantsInstances = {targetCharacter, localCharacter}
                raycastParams.FilterType = Enum.RaycastFilterType.Exclude

                local finalPosition = intendedPos
                local raycastResult = workspace:Raycast(targetPos, rayDirection, raycastParams)
                
                if raycastResult then
                    finalPosition = raycastResult.Position - (rayDirection.Unit * 1.5)
                end
                
                if AimbotEnabled and targetHead then
                    localRoot.CFrame = CFrame.lookAt(finalPosition, targetHead.Position)
                else
                    localRoot.CFrame = CFrame.new(finalPosition)
                end
            end
        else
            if WasHovering then
                restoreCharacter()
                WasHovering = false
            end
            
            if targetCharacter and targetCharacter:FindFirstChild("KillHighlight") then
                targetCharacter.KillHighlight:Destroy()
            end
        end
    end
end)
